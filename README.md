منصة إشعارك – نموذج تجريبي

منصة إشعارك هي نموذج أولي (Prototype) يهدف إلى محاكاة خدمة حماية المستخدم من الروابط المشبوهة، مع واجهة استخدام بسيطة تدعم كبار السن وذوي الهمم، وبأسلوب بصري قريب من هوية منصة أبشر.
تم تصميم النموذج ليكون قابلًا للتطوير والدمج مع خدمات فعلية مستقبلًا.

🎯 أهداف المنصة

توفير تجربة استخدام واضحة وسهلة للجميع.
محاكاة فكرة الفحص الأمني للروابط بأسلوب مبسّط.
تقديم واجهة متوافقة بصريًا مع هوية منصة أبشر.
دعم الوصول الرقمي لكبار السن وذوي الهمم (تكبير–تباين–صوت).
بناء نموذج قابل للتوسّع والربط مستقبلاً بأنظمة ذكاء اصطناعي.
🧩 مبررات اختيار التقنية

تم استخدام HTML / CSS / JavaScript لأنها:

خفيفة وسهلة التطوير والتعديل.
تعمل مباشرة من المتصفح بدون خوادم (Serverless).
مناسبة للنماذج التجريبية والعروض التوضيحية.
تُنشر بسهولة عبر GitHub Pages بشكل مجاني.
تسهّل على أعضاء اللجنة تصور التجربة الفعلية للمستخدم.
تدعم بسهولة ميزات الوصول (Accessibility).
⚙️ نموذج التشغيل (Operating Model)

يعتمد النموذج على صفحات ثابتة تعمل مباشرة داخل المتصفح:

الصفحات الأساسية

الصفحة الرئيسية
صفحة فحص الرابط
صفحة النتيجة (ضمن فحص الرابط)
صفحة البلاغات
صفحة المساعد الصوتي والنصي
آلية فحص الرابط (محاكاة)

يتم من خلال JavaScript:

قراءة الرابط المدخل.
مقارنته بكلمات تُستخدم في الروابط الاحتيالية (مثل login, bank, verify).
عرض النتيجة: آمن – مشتبه – خطر.
لا يتم إرسال أي بيانات خارج المتصفح.
ميزات الوصول

تكبير النص.
تفعيل التباين العالي.
قراءة الردود صوتيًا عبر Web Speech API.
مساعد نصي/صوتي يقدم إرشادات مبسطة.
🔮 التوسّع المستقبلي (Scalability)

يمكن تحويل المنصة إلى نظام فعلي عبر:

ربطها بـ واجهات (APIs) معتمدة لفحص الروابط.
إضافة محرك ذكاء اصطناعي لتحليل الأنماط الأمنية للروابط.
إنشاء نظام بلاغات حقيقي بربط المنصة بقاعدة بيانات.
تحليل السجل التاريخي لعمليات الفحص.
إضافة Dashboard للمشرفين.
📁 هيكلة المشروع

index.html ← الصفحة الرئيسية check.html ← فحص الروابط report.html ← رفع البلاغات chat.html ← المساعد النصي/الصوتي style.css ← التصاميم app.js ← التفاعلات icons.svg ← (أيقونات (اختياري

🚀 طريقة الاستخدام (User Journey)

يفتح المستخدم الصفحة الرئيسية.
يختار خدمة فحص رابط.
يدخل الرابط المطلوب.
يتم تحليل الرابط وعرض النتيجة فورًا.
يمكنه رفع بلاغ أو استخدام المساعد الصوتي.
يمكنه تفعيل التكبير أو التباين أو القراءة الصوتية.
👥 فريق الابتكار والتطوير

تم إعداد هذا النموذج لعرض الفكرة التقنية بشكل واضح، وتوضيح قابلية التوسّع مستقبلًا نحو نظام حماية رقمي متكامل.

📌 ملاحظة

هذا النموذج للعرض فقط ولا يرسل أي بيانات خارج المتصفح، ولا يقوم بفحص أمني حقيقي.
